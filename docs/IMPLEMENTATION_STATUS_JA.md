# アプリ構築進捗

- 更新日: 2026-07-16
- 基準: `main` / `afabafcedb3ba2a35e3e9de3f9508923119412b8`
- 評価方法: 見た目の完成度ではなく、実データを安全に扱い、依頼者へ引き渡せる状態を基準にする

## 結論

| 評価対象 | 現在の進捗 |
| --- | ---: |
| 設計・匿名PoC | **85〜90%** |
| 最初の本番「作業員名簿MVP」 | **約20%** |
| 日報・月次・出面まで含む製品全体 | **約10〜15%** |
| 現在そのまま実データを入れて利用できる本番環境 | **0%** |

画面とPDFの匿名PoCは高い完成度にある。一方、本番DB、認証、MFA、RLS、顧客所有環境、運用・引渡しは未実装であるため、PoCの完成度をそのまま本番進捗として数えない。

## 領域別

| 領域 | 進捗 | 現在地 |
| --- | ---: | --- |
| 要件・アーキテクチャ | **75%** | 業務範囲、自社作業員限定、公式様式、顧客所有・本番非アクセス方針、セキュリティ基準を整理。保持期間、利用者数、最終権限表が未決定 |
| 名簿UI PoC | **90%** | 5工程、CRUD、不足修正、ドラフト、未保存確認、スマホ幅、文字拡大、キーボード、ARIAを匿名データで確認済み |
| PDF PoC | **95%** | A3横、8名＋1名、日本語、背景一致、長文、0名拒否、HTMLエスケープを10テストで確認済み |
| 本番アプリ基盤 | **0%実装 / 20%設計** | 本番用Vite/React/TypeScript、CI、専用非公開リポジトリは未作成 |
| 認証・DB・セキュリティ | **0%実装 / 30%設計** | 顧客所有、MFA、RLS、秘密管理、P0基準は文書化。migration、Auth、RLS、監査、バックアップは未実装 |
| 名簿の本番縦断機能 | **25%** | UIとPDFの両PoCは成立したが、DB・認証・認可・動的PDFで接続されていない |
| 日報・月次・出面 | **10%** | 項目、計算規則、旧HTML試作は存在。本番コード、DB、正式帳票、テストは未着手 |
| 本番配備・顧客引渡し | **10%** | GitHubレビュー運用は成立。顧客所有アカウント、権限削除、鍵交換、復旧・事故対応は未実施 |

## 完了しているもの

- スマートフォンUIの方向性
- 作業員、現場、現場別作業員情報の分離
- 自社1社・自社作業員だけを扱う初期範囲
- 現場選択からPDF確認までの名簿作成UX
- 不足項目から正しい編集先へ戻る操作
- 国交省作業員名簿の匿名PDF生成PoC
- A3横、8名単位、9名2ページ、日本語、背景の検証
- 360px、390px、430px、文字200%相当、仮想キーボード相当のUI検証
- 顧客所有の本番環境と、諒さんが本番データへアクセスしない方針
- 本番セキュリティP0ゲートの定義・文書化

## 未実装の本番機能

- 本番用アプリプロジェクト
- 顧客所有の非公開GitHub、ホスティング、Supabase
- 招待制ログインとMFA
- Postgres schema、migration、RLS、監査ログ
- 作業員・現場・現場別情報のクラウドCRUD
- UI入力からの動的PDF生成
- client-side PDFの品質検証
- バックアップ、復元、保存期限、削除処理
- 顧客受入、開発者権限削除、秘密情報ローテーション
- 日報、月次日報、出面表の本番実装

## 次の到達点

次は「名簿MVP Phase 1〜2」を進める。

1. 本番用フロントエンド基盤とCIを匿名データで作る。
2. Supabase migration、招待制Auth、MFA、RLS、監査ログを作る。
3. 未認証、権限外、合成別organizationの拒否テストを通す。
4. 作業員・現場・現場別情報の最小CRUDへ進む。

本番コードは最終的に依頼者所有の非公開リポジトリへ移し、P0完了後にだけ実データを登録する。
